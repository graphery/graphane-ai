// https://platform.openai.com/docs/guides/text-generation/building-prompts

import OpenAI from "openai";

const openai     = new OpenAI();
const completion = await openai.chat.completions.create({
  model    : "gpt-4o-mini",
  messages : [
    // Messages with the system role act as top-level instructions to the model, and typically
    // describe what the model is supposed to do and how it should generally behave and respond.
    {
      role    : "system",
      content : [
        {
          type : "text",
          text : "eres un humorista que cuenta chistes e historias graciosas"
        }
      ]
    },
    // Messages with the assistant role are presumed to have been generated by the model, perhaps
    // in a previous generation request. They can also be used to provide examples to the model for
    // how it should respond to the current request - a technique known as few-shot learning.
    {
      "role"    : "assistant",
      "content" : [
        {
          type : "text",
          text : "Hay 10 tipos de personas en este mundo. Los que entienden binario y los que no."
        },
        {
          type : 'text',
          text : 'Lo mejor de Boolean es que incluso si te equivocas, solo te equivocas un poco.'
        },
        {
          type : 'text',
          text : 'Un hijo le preguntó a su padre (un programador) por qué el sol sale por el este y se pone por el oeste. ¿Su respuesta? ¡Funciona, no lo toques!'
        },
        {
          type : 'text',
          text : '¿Qué es un algoritmo? Una palabra que usan los programadores cuando no quieren explicar lo que hicieron.'
        },
        {
          type : 'text',
          text : 'Si la depuración es el proceso de eliminar errores, entonces la programación debe ser el proceso de colocarlos.'
        },
        {
          type : 'text',
          text : '¿La mejor manera de depurar un código? Borra el código 😅'
        },
        {
          type : 'text',
          text : '¿Cuál es la forma orientada a objetos para volverse rico? Herencia 🤣'
        },
        {
          type : 'text',
          text : 'El programador se quedó atascado en la ducha debido a las instrucciones del estado del champú: hacer espuma, enjuagar, repetir'
        },
        {
          type : 'text',
          text : '¿Qué le dijo el código Java al código C? No tienes clase. 😅'
        }
      ]
    },
    // User messages contain instructions that request a particular type of output from the model.
    // You can think of user messages as the messages you might type in to ChatGPT as an end user.
    {
      role    : "user",
      content : [
        {
          type : "text",
          text : "Cuéntame un chiste sobre programadores"
        }
      ]
    }
  ]
});
console.log(completion.choices[0].message.content);
